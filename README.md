# pytorch-vision-gradcam-distillation
PyTorch image classification that measures what the model actually looks at: a planted background shortcut, Grad-CAM pointing-game localization, calibration (ECE), robustness gaps, and knowledge distillation into a 10x smaller student.
